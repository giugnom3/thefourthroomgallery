{
  "name": "the-fourth-room-gallery",
  "version": "1.0.0",
  "homepage": "https://giugnom3.github.io/thefourthroomgallery/",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "start:powershell": "$env:NODE_OPTIONS='--openssl-legacy-provider'; react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider && echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "nipplejs": "^0.10.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "^3.0.1",
    "three": "^0.176.0",
    "three.js": "^0.77.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "auto": "^11.3.0"
  }
}
